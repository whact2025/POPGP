# Falsification matrix

Active or explicitly negative gate rows carry stable IDs in the table itself and are
linked to demonstrated controls in `GATE_TEST_REGISTRY.md`. Rows without IDs are
explicitly ungated: they do not yet have an implemented acceptance gate.

| Hypothesis | Controlled family and observable | Expected behavior | Failure threshold | Confounders / requirements | Current readiness |
|---|---|---|---|---|---|
| Source response under a non-affine KMS family (`GATE-SOURCE-KMS-RESPONSE`) | `ρ(ε)∝exp[-β(H+εV)]`; `D`, signed `Δ⟨K⟩`, `Δ⟨H⟩`, and Φ amplitude | `D/ε²` converges to the Kubo--Mori coefficient; a proposed linear candidate has a nonzero susceptibility | Relative coefficient drift >`1e-3`, slope outside `2±0.02`, normalized RMSE >`1e-2`, signal/floor <`1000`, first-order negative control accepted, or Richardson susceptibility ≤10 total errors | Excitation family, faithful reference, conserved charges, solver linearity, asymptotic-window selection | For localized `V=-h_center`, all direct gates pass and numerical coefficients match Kubo--Mori values across Heisenberg/Ising, β≤3 (including 2.5), and N∈{3,5,7} in declared subsets. Physical-energy response follows from the exact KMS identity. Under affine mixtures unit slopes are identities; isospectral controls gate only `ΔS=0` and `D=Δ⟨K⟩` at roundoff scale. |
| Equal energy, different entropy (`GATE-ENTROPY-CONFOUND`) | KMS reference; states matched in energy/charges/localization | Far-field monopole insensitive to entropy alone | Source differs beyond matched-energy tolerance without additional physical charge | Exact energy matching, finite-size degeneracy | Analytic finite control complete; raw RE differs. Larger localized control needed. |
| Modular-energy localization (`GATE-MODULAR-LOCALIZATION`) | Compare one-site reduced `Δ⟨K_i⟩` with `βΔ⟨h_i⟩` and global `Δ⟨K⟩` | Local candidate is nonzero, its `−βΔ⟨h_i⟩` density sums to `−Δ⟨K⟩`, and the local-energy profile spreads under the declared noncommuting quench | Zero response to a finite-energy excitation, failure of the modular-density sum, or absence of profile spreading in the declared quench. Constancy of the measured global Hamiltonian is an implementation-consistency identity, not a source-law falsifier. | Local modular Hamiltonians can be identity-like; microscopic energy split is non-unique | Reduced-state candidate fails the symmetric-chain control. Exact-backend `−βΔ⟨h_i⟩` passes finite-chain modular-sum and profile-spreading tests but is a supplied convention. Evolution preserves the measured global Hamiltonian by construction; no discrete continuity current or local conservation law is implemented. |
| Blind topology (`GATE-BLIND-TOPOLOGY`) | Chains, square/triangular/3D lattices; edge precision/recall and distance distortion | High recovery without ground-truth access | Pre-register by family; current target P,R≥0.9 | Temperature, size, threshold, long-range correlations | Chain/grid pilot and permutation test pass. Other families open. |
| Non-geometric controls (`GATE-NONGEOMETRIC-CONTROLS`) | Bell pairs, expanders, random regular and shuffled MI matrices | No stable low-D manifold declaration | False low-D selection or “separable” edge claim across perturbations | MDS can visually flatten arbitrary finite graphs | The uniform control is non-separable. Disjoint-Bell non-separability holds only at exact MI degeneracy: evolution to `dt=0.05` produces a separable declaration with gap ratio about `1.03e5` while retaining the false D*=1 `geometric_candidate` status. Petersen is non-geometric at default but exposes λ sensitivity. |
| Parameter robustness (`GATE-PARAMETER-ROBUSTNESS`) | Sweep β, I0 multiplier, gap ratio, λ, probes, seed | Qualitative result survives a predeclared region | Target appears only at isolated/post-selected values | Correlated hyperparameters and phase transitions | The non-affine KMS response sweep passes direct order, precision-floor, negative-control, Richardson, and Kubo--Mori comparison gates for two interactions through β=3 (including 2.5) and three small odd sizes. Affine linear-observable legs are excluded as identities. Five probe seeds preserve chain partition. Scalable refinement and broader geometry sweeps remain open. |
| Cross-platform regeneration (`GATE-PORTABLE-ARTIFACT-CONTRACT`) | Locked Windows/Linux environments; structured result contracts and raster visual envelopes | Exact scientific structure/gates and bounded numeric diagnostics agree; after canonicalizing a declared near-zero legend, every raster channel differs by at most 4 | Type, shape, identity, criterion, Boolean outcome, stable input, visual geometry, or any per-channel raster difference >4 | Same lock and candidate; bound of 4 is calibrated against retained honest Windows/Linux artifacts; compact/line/dash/text/real-annotation attacks must reject | Review and re-review localized feature, thin/disconnected curve, text, and real-annotation removals are executable negative controls. Fresh two-platform adversarial execution is required before a viability claim. |
| Structured scientific decisions (`GATE-DECISION-PREDICATE-INTEGRITY`) | Every registered example check, lowest-level retained raw array, duplicated pipeline/check alias, fit, identity, global-energy statistic, endpoint statistic, sensitivity case, and precision gate | Serialized summaries and `passed` values equal independently recomputed quantities from finite, shape-valid, ordered raw operands | Any raw element or alias crosses its criterion, a fit/identity/ratio is stale, an input is malformed or non-finite, or the serialized Boolean remains stale | All simple-source and many-body fits, simple and many-body first-law identities, quadratic/Richardson assessments, KMS/static/evolved local-global decompositions, evolved-energy/endpoint metrics, isospectral/commuting controls, KMS density, all twelve sensitivity cases, every retained clock-potential summary, graph/source/config provenance relation, finite-graph solver residual, and gravity radial/log-fit/redshift derivative are recomputed | Threshold-crossing raw mutations, paired sensitivity aliases, correlated evolved-global shifts, every evolved local-profile and retained potential element, potential scale/sign/permutation/uniform-shift transformations, diagonal/negative/asymmetric/new-edge graph mutations, correlated graph/source changes, mass branch changes, source-background and non-zero-sum attacks, later Richardson-array points, malformed shapes/order/non-finite values, and prior `phi_range`/pipeline/nested-floor counterexamples are executable negative controls. The grid placeholder gate requires both `phi_range < 1e-12` and `max(abs(phi)) < 1e-12`; clock potentials must use an MI/edge-derived symmetric nonnegative zero-diagonal graph, a raw-source/policy-derived effective source, the configured solver mass/gauge, and the retained graph equation. Fresh review remains required. |
| Frozen repository bytes (`GATE-REPOSITORY-BYTE-INTEGRITY`) | Literal worktree bytes/modes versus frozen Git objects before and after every child | Only explicitly declared generated artifact paths may differ | Actual tracked byte/mode change, staged state, nonordinary index flag, clean-filter concealment, undeclared ordinary/ignored residue, executable bytecode, deletion, rename, or symlink | Comparison uses `git ls-tree` plus batched `git cat-file`, not index stat/filter semantics; every cache and environment is created outside the checkout | Assume-unchanged, clean-filter, self-restoring source, ignored-bytecode, and residue bypasses are executable negative controls. Fresh Windows/Linux execution remains required. |
| Locked Python startup surface (`GATE-STARTUP-SURFACE-INTEGRITY`) | Fresh external non-editable environment, complete byte manifest, trusted pre/post wrapper, and site-disabled isolated Python | Every environment file/symlink stays hash-bound and no `.pth`, customize, or modified dependency code can execute undetected | Any environment byte changes, carrier marker executes, manifest mismatch, blocked environment variable, non-base verifier, internal bytecode cache, or dirty postflight | Checker is extracted from the frozen Git object with base `python -I -S`; target uses `-I -S`, an external bytecode cache, and direct site-package paths, never `site.addsitedir` | Persistent, self-deleting, byte-restoring, and installed-package carrier families are rejected before execution; measured lock-installed hooks are inert. Fresh Windows/Linux execution remains required. |
| Hosted protocol snapshot identity (`GATE-RTHREE-SNAPSHOT-IDENTITY`) | Manual hosted dispatch from an exact content-addressed tag; signed platform and mutation outputs; final packet assembly | The dispatch tag, resolved commit, `github.sha`, checkout HEAD, runner and mutation source commit, Sigstore source digest, assembler argument, packet protocol commit, and validator expectation are one identical 40-hex commit, with one producer run ID and attempt | Any automatic event, branch/lifecycle ref, tag/commit/HEAD disagreement, wrong or cross-run attestation, mixed producer identity, or attempt to emit a commitment after an identity mismatch | The final reviewed protocol snapshot must already exist as an immutable Git commit; its tag name is `popgp-via000-r3-protocol-<40hex>` and must resolve to that suffix exactly | R3 guard, workflow, assembler, raw schema, mutation runner, and validator controls reject identity drift before output commitment. The campaign remains drafted and requires independent review, a final snapshot/tag, and fresh custody verification before holdout. |
| Refinement convergence | Same physical geometry at growing N | Observables converge with stated order/error | No stable window or drifting inferred dimension | Boundary conditions and scale matching | Not currently feasible with exact backend alone. |
| Clock/spatial consistency | Independent clock simulation versus solved Φ | `dτ` contrasts match predicted `exp(Φ)` | Sign/magnitude mismatch beyond error | `dt` currently substitutes for action distance | Sign convention tested; independent clock observable absent. |
| Lorentz recovery | Excitation dispersion by direction and boost-like transformations | Common limiting speed and vanishing IR anisotropy | Persistent preferred-frame signal | Finite lattice artifacts, gauge choice | Not implemented. |
| Causal completeness | Controlled high-curvature evolution and causal trajectories | Deterministic extendibility or defined phase transition | Evolution terminates or trajectories cannot extend | Must identify modified singularity-theorem assumption | Not implemented. |
| Closure / conservation | Regge geometric tensor, source tensor, divergence residual | Closure and discrete Bianchi residual converge to zero | Residual fails to decrease under refinement | Boundary terms, dual volumes, gauge | The global microscopic energy is conserved under a declared local decomposition whose site profile spreads. This is an implementation-consistency identity; no local conservation law, discrete continuity current, or covariant/discrete Bianchi closure is implemented. |

Negative results must remain in version control with the same prominence as passes.
Changing the source law, inference rule, or acceptance threshold requires a recorded
decision and a rerun of all relevant controls.
